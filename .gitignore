target/
*.class
*.jar
*.war
*.ear
*.log
.idea/
*.iml
.classpath
.project
.settings/
/backend-service/logs/
node_modules/

# Environment files with secrets
.env
.env.mqtt
.env.local
.env.*.local
!.env.mqtt.example
.env.mqtt.hene
docker-compose-mqtt-hene.yml

# Windows reserved-name artifact (e.g. from a stray `> NUL` redirect)
/NUL

# Exportierte Docker-Image-Archive (siehe scripts/build-pi-images.ps1)
zev-images-*.tar.gz

# Aus Specs generierte PDFs (jederzeit aus der .md neu erzeugbar)
/Specs/*.pdf

# Root-Zertifikat von Caddys interner CA (Specs/HTTPS.md, Schritt 3). Es entsteht je
# Umgebung neu und wird bei jedem Verlust des Volumes caddy-data neu erzeugt - deshalb
# jederzeit aus dem Container holbar und nicht versionierbar.
caddy-root.crt
