# ZEV Pi-Gateway – Laufzeit-Abhängigkeiten
# Installation (auf dem Raspberry Pi, im venv):
#   pip install -r requirements.txt
#
# Versionen bewusst als untere Schranke (>=) mit Obergrenze, damit auf dem
# Pi reproduzierbar, aber sicherheitsrelevante Patches noch möglich sind.

# Modbus-TCP-Client (Wago-Zähler)
# Obergrenze <3.10: ab 3.10.0 wurde der Client-Parameter slave= zu device_id=
# umbenannt (siehe modbus_reader.py). Vor einem Bump den Reader anpassen.
pymodbus>=3.6,<3.10

# MQTT-Client (Publish der Zählerstände)
paho-mqtt>=2.1,<3.0

# YAML-Konfiguration
PyYAML>=6.0,<7.0
